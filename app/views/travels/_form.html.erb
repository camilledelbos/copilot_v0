<%= minimal_form_for(@travel) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, :autofocus => true, placeholder: 'Travel title'%>
  </div>

  <div class="form-actions">
    <%= f.button :submit, label: 'Create a Travel' %>
  </div>
<% end %>
