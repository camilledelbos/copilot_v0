<tbody id="stage" data-update-url="<%= sort_stages_url %>">
    <% @stages.each do |stage| %>
      <tr>
        <%= content_tag_for :td, stage do %>
            <span class="handle">[drag]</span>
            <%= link_to h(stage.address), (@stage) %>
        <% end %>
          <td>
            <%= stage.duration %>
          </td>
            <td><%= button_to '-', stages_path, remote: true, method: :delete, data: { confirm: 'Are you sure?' } %>
            </td>
      </tr>
     <% end %>
</tbody>
